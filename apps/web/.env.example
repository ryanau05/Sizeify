# Base URL of the demo API (api.demo.app running with DEMO_MODE=1).
VITE_API_BASE=http://localhost:8000

# Pre-seeded demo user JWT, printed by `python -m api.demo.seed_demo`.
# Lets the demo skip a login screen and go straight to the magic moment.
VITE_DEMO_JWT=
